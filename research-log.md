# Research Log

Chronological record of research decisions and actions. Append-only.

| # | Date | Type | Summary |
|---|------|------|---------|
| 1 | 2026-05-13 | bootstrap | Scoped the project to a feasible 2-week pilot on LoRA fine-tuning stability/quality rather than full pretraining. Selected the first research question: whether selective fp32 precision islands, starting with normalization layers, improve stability or validation loss over standard bf16 mixed precision under limited GPU resources. Created H1 protocol before writing code. |
| 2 | 2026-05-13 | inner-loop | Converted H1 into an executable first experiment scaffold. Added mandatory dtype-probe phase to prevent testing a no-op precision policy, created dtype probe and minimal LoRA runner scripts, and documented smoke-test commands. No experiment results yet. |
| 3 | 2026-05-13 | bootstrap | Bootstrapped literature survey for mixed precision in LLM LoRA fine-tuning. Summarized 9 key papers under literature/ and created literature/survey.md. Main gaps: normalization dtype in LoRA fine-tuning, internal accumulator dtype visibility, stability metrics under limited-GPU budgets, and whether precision islands matter only in stressed regimes. No new experiments run. |
| 4 | 2026-05-13 | inner-loop | Upgraded the H1 LoRA runner from smoke-only defaults to protocol-faithful defaults: 8,000 training examples, 1,000 validation examples, full validation by default, final eval loss in summary, max grad norm, effective batch size, and train-only throughput metrics. One-step CUDA development checks passed for both bf16_baseline and fp32_norms. No 1,000-step H1 run has been started yet. |
| 5 | 2026-05-13 | research-direction | Integrated the adaptive precision assignment idea into the research plan as H6. H6 reframes the broader goal as deriving per-operation precision from stability signals, while keeping H1 as the required calibration experiment. Added a protocol for signal instrumentation, perturbation-based calibration, precision promote/demote decisions, and matched comparison against static policies. |
| 6 | 2026-05-13 | literature | Refreshed the literature phase for adaptive precision assignment from stability signals. Added paper notes for INT8 outlier-aware inference, SmoothQuant, ZeroQuant, HAWQ/HAWQ-V3, AdaPT, convergence-aware operator-wise mixed precision, FP4 LLM training, SNIP adaptive subbyte LLM training, Attn-QAT, and BitNet b1.58. Updated survey, open problems, and evidence gaps. No experiments were run. |
| 7 | 2026-05-13 | inner-loop | Turned H6 from a protocol into an executable signal-only calibration study. Added `probe_stability_signals.py`, `study_plan.md`, and `analysis.md`. Ran a one-batch CUDA smoke on Qwen/Qwen2.5-0.5B over the first eight candidate modules. The smoke produced stability signals and a frozen policy trace: seven projection modules stayed bf16, layer-0 input RMSNorm was promoted to fp32, mean calibration loss was 1.7697, NaN/Inf count was 0, and peak CUDA memory was 2.1825 GiB. |
| 8 | 2026-05-14 | outer-loop | Reframed H6 as the trunk research hypothesis rather than a late follow-on. Kept H1 and H5 as required foundations for H6, because H6 needs a matched static anchor and dtype-validity evidence. Reclassified H2, H3, and H4 as conditional ablations to run only if H6 needs a stronger static anchor, stressed regime, or variance confirmation. |
| 9 | 2026-05-14 | results | Synthesized completed H1 training artifacts. Seed-42 bf16 baseline reached validation NLL 2.09066; matched fp32_norms reached 2.08642, a 0.00425 absolute improvement (about 0.20%) with zero loss spikes and zero NaN/Inf events in both runs. Throughput overhead was about 0.83%, below the 10% cost limit, but the quality delta is below the locked 1% support threshold, so H1 is inconclusive in the default regime. Additional bf16 baseline seeds 43 and 44 reached 2.06876 and 2.09593, showing baseline seed variation larger than the seed-42 fp32_norms delta. |
| 10 | 2026-05-14 | dtype-validity | Added and ran an H5 RMSNorm internal dtype probe. CUDA was unavailable during this micro-probe, so the execution path was CPU, but the probe recorded the installed `Qwen2RMSNorm.forward` source and a matching reference trace. The implementation casts `hidden_states` to fp32 before `pow`, `mean`, and `rsqrt`; reference output matched actual output with max absolute difference 0.0. Combined with the earlier CUDA boundary dtype probe, this suggests Qwen RMSNorm reductions are already fp32 internally under baseline bf16 boundaries, weakening H1's original mechanistic rationale. |
| 11 | 2026-05-14 | outer-loop | Folded the brainstormed idea into the main research state and simplified H6. The project question is now: before LoRA fine-tuning, can a short precision check identify fragile modules, freeze a mixed-precision policy, and match bf16 quality while saving resources or expanding the stable training envelope? This reframes H1 as a weak static anchor and H5 as the reason hand-picked norm islands are insufficient. The next decisive experiment is perturbation-calibrated module ranking: test whether cheap signal-only measures predict one-island loss deltas before trusting any policy. |
| 12 | 2026-05-14 | results | Analyzed H6 Stage 1 bf16 calibration for seeds 42, 43, and 44. Each run observed 218 modules with zero NaN/Inf events, peak CUDA memory 2.7303 GiB, and highly stable decisions: 217/218 modules had identical assignments across seeds. The only unstable int8 candidate was `layers.23.mlp.gate_proj`, which crossed the threshold only for seed 42. The conservative policy leaves all attention projections bf16, promotes all norms/logits to fp32, and leaves nearly all MLP projections bf16. Stage 1 is therefore stable enough for perturbation testing but too conservative to freeze a resource-saving policy. |
| 13 | 2026-05-15 | results | Analyzed the first H6 Stage 2 perturbation probe on seed 42. Fake int8 output perturbation produced large loss increases for the three Stage-1 high-risk MLP down projections: +0.2867 for `layers.21.mlp.down_proj`, +0.2228 for `layers.2.mlp.down_proj`, and +0.1491 for `layers.3.mlp.down_proj`. The four relaxed low-risk late-layer MLP gate/up candidates had near-zero deltas between about -0.0042 and +0.0070. For MLP projections, Stage 1 max outlier score correlates with absolute perturbation loss delta at about 0.91, and max int8 relative MSE at about 0.81. This is positive evidence that cheap calibration signals predict projection sensitivity; replicate on seeds 43 and 44 before freezing a policy. |
| 14 | 2026-05-15 | results | Replicated H6 Stage 2 perturbation probes on seeds 43 and 44 and synthesized all three seeds. The same three high-risk MLP down projections had large positive int8 perturbation deltas in every seed, with cross-seed mean deltas of +0.3699 for `layers.2.mlp.down_proj`, +0.2665 for `layers.21.mlp.down_proj`, and +0.2209 for `layers.3.mlp.down_proj`. The four late-layer gate/up candidates stayed near zero, with worst-case absolute mean delta at most 0.0070. Across 21 MLP perturbations, max outlier score correlates with absolute loss delta at about 0.91. Next step: freeze a narrow H6 policy around `layers.22/23.mlp.gate_proj` and `layers.22/23.mlp.up_proj`, then run a short training comparison against bf16. |
| 15 | 2026-05-15 | results | Ran the first frozen H6 narrow-policy 500-step training comparison against the matched bf16 seed-42 control. The H6 fake-int8 late-layer MLP policy reached final eval loss 1.63112 versus bf16 1.62949, a +0.00163 degradation or about +0.10%, inside the locked 1% quality gate. Both runs had zero loss spikes and zero NaN/Inf events, and max grad norm was unchanged. Peak memory was essentially unchanged, while train throughput fell by about 13.5% because the current implementation uses Python-level fake-quant hooks. Conclusion: H6 has positive evidence for calibration-guided quality-preserving module selection, but no resource-saving claim yet. |
| 16 | 2026-05-15 | results | Completed the paired H6 narrow-policy 500-step comparison across seeds 42, 43, and 44. BF16 eval losses were 1.62949, 1.63444, and 1.63247; H6 eval losses were 1.63112, 1.63621, and 1.63493. The paired H6 degradations were +0.100%, +0.108%, and +0.151%, all well inside the locked 1% quality gate. Both policies had zero total loss spikes and zero total NaN/Inf events. Peak memory was effectively unchanged and H6 mean train throughput was about 5.8% lower under Python fake-quant hooks, so the result supports quality-preserving sensitivity assignment but not real resource savings yet. |
| 17 | 2026-05-16 | implementation | Locked and implemented H6.1, a SNIP-style budgeted policy expansion. Added `build_snip_style_policy.py` to aggregate Stage 1 calibration and Stage 2 perturbation artifacts into conservative module risk scores, then emit frozen `k=4/8/16/24` fake-int8 module lists. The default `k=4` policy is anchored to the already validated H6 late-layer MLP policy, while wider budgets add ranked low-risk gate/up projections. Added `run_h6_snip_style_width_sweep.sh` for seed-42 screens and follow-up seed replication. A one-step CUDA smoke confirmed the generated `k=4` modules resolve and train with zero NaN/Inf events. |
| 18 | 2026-05-16 | results | Completed the H6.1 seed-42 SNIP-style width screen. Matched bf16 reached eval loss 1.62978. The k=4, k=8, k=16, and k=24 fake-int8 MLP gate/up policies reached 1.63111, 1.63173, 1.63126, and 1.63177 respectively, corresponding to relative degradations of +0.082%, +0.120%, +0.091%, and +0.122%. All policies had zero loss spikes and zero NaN/Inf events, with max grad norms close to bf16. This supports widening the calibration-selected policy at least to 24 MLP gate/up modules on seed 42. Throughput and memory remain noisy under Python fake-quant hooks, so this is still a quality/stability result rather than a hardware resource-saving result. Next step: replicate the widest safe policy, k=24, on seeds 43 and 44. |
| 19 | 2026-05-17 | results | Completed the H6.1 k=24 replication on seeds 43 and 44 using per-device batch size 2 and gradient accumulation 8, preserving effective batch size 16 with matched bf16 controls. Across seeds 42, 43, and 44, bf16 eval losses were 1.62978, 1.61701, and 1.62089; k=24 eval losses were 1.63177, 1.61975, and 1.62248. The paired relative degradations were +0.122%, +0.169%, and +0.098%, with mean +0.130%, all far inside the locked 1% quality gate. Both policies had zero loss spikes and zero NaN/Inf events in every run. The batch-size-2 runs roughly doubled throughput relative to the earlier batch-size-1 seed-42 run, but k=24 remained about 3-4% slower than matched bf16 and used about 0.009 GiB more peak memory, so the claim remains quality/stability-preserving policy expansion rather than resource savings. |
| 20 | 2026-05-17 | implementation | Locked and implemented H6.2 hardware-realistic low-precision feasibility. Added a bitsandbytes 8-bit LoRA policy (`lora_8bit_int8`) to the shared runner alongside the existing QLoRA 4-bit NF4 path. Added `run_h6_hardware_resource_matrix.sh` to run matched bf16, H6.1 fake-int8 k=24, QLoRA 4-bit NF4, and 8-bit LoRA under the same microbatching and hardware label. Updated the resource summarizer to compare all available policies against matched bf16 instead of only QLoRA. |
| 21 | 2026-05-17 | results | Completed the H6.2 100-step hardware-realistic feasibility screen on the lab RTX 3090 (`cuda_device_name=NVIDIA GeForce RTX 3090`, `CUDA_VISIBLE_DEVICES=3`) with per-device batch size 2 and gradient accumulation 8. Matched bf16 reached eval loss 1.64161, peak memory 4.530 GiB, and 897.1 train tokens/sec. Fake-int8 k=24 stayed inside the 1% quality gate at +0.268% eval loss but used slightly more memory (+0.20%) and was slower (-2.16%). bitsandbytes 8-bit LoRA stayed inside the quality gate at +0.102% eval loss but used more memory (+16.8%) and was much slower (-40.2%). QLoRA 4-bit NF4 failed the quality gate at +2.83% eval loss and also used more memory (+15.8%) and was slower (-37.6%). All policies had zero loss spikes and zero NaN/Inf events. Conclusion: H6.2 is negative for resource savings on this 3090/bitsandbytes setup; the current contribution should remain framed as calibration-guided sensitivity ranking unless a different hardware-backed implementation is tested. |
| 22 | 2026-05-17 | results | Completed the H6.3 7B hardware scale screen on the lab RTX 3090 with Qwen/Qwen2.5-7B, per-device batch size 1, gradient accumulation 16, effective batch size 16, and 100 optimizer steps. Matched bf16 reached eval loss 1.39876, peak memory 19.448 GiB, and 191.2 train tokens/sec excluding the first step. bitsandbytes 8-bit LoRA stayed inside the 1% quality gate at +0.108% eval loss and saved 6.70% peak memory, but was 40.53% slower. QLoRA 4-bit NF4 also stayed inside the quality gate at +0.709% eval loss and saved 23.32% peak memory, but was 19.78% slower. All policies had zero loss spikes and zero NaN/Inf events. The 8-bit run emitted bitsandbytes warnings that fp32/bf16 inputs are cast to fp16 inside `MatMul8bitLt`, so the result should be described as int8 weights with fp16 activation matmul behavior rather than pure bf16 compute. |
| 23 | 2026-05-18 | results | Completed the H6.3 7B 500-step QLoRA follow-up on seed 42 against a matched bf16 control. BF16 reached eval loss 1.37747, peak memory 19.448 GiB, and 191.6 train tokens/sec excluding the first step. QLoRA 4-bit NF4 reached eval loss 1.38524, a +0.563% degradation that remains inside the 1% quality gate, while saving 23.32% peak memory and running 19.95% slower. Both runs had zero loss spikes and zero NaN/Inf events. Conclusion: the 7B QLoRA memory-capacity trade-off persists for 500 steps and should now be replicated on seeds 43 and 44. |
| 24 | 2026-05-18 | results | Completed the H6.3 7B 500-step QLoRA replication on seeds 43 and 44. Across seeds 42, 43, and 44, paired QLoRA eval-loss degradations were +0.564%, +0.566%, and +0.927%, with mean +0.686%; all stayed inside the locked 1% quality gate. Peak-memory savings were stable at -23.32%, and throughput was consistently about 20% slower than matched bf16. All six runs had zero loss spikes and zero NaN/Inf events. Conclusion: H6.3 is supported as a robust memory-capacity trade-off for Qwen2.5-7B LoRA on the lab RTX 3090, but not as a speed improvement. |
| 25 | 2026-05-19 | report | Created the progress report `to_human/progress-2026-05-19-h6-report.html`. The report frames the project around two supported findings: calibration-guided module sensitivity ranking at 0.5B, and a robust Qwen2.5-7B QLoRA memory-capacity trade-off on RTX 3090. It recommends stopping generic bitsandbytes repeats and next testing whether the calibration-guided sensitivity ranking transfers from 0.5B to 7B. |
| 26 | 2026-05-19 | results | Completed the first H6.4 Qwen2.5-7B calibration-transfer probe on seed 42 with a targeted 14-module panel. Stage 1 assigned all projections to bf16 and all norm/logit paths to fp32, indicating the 0.5B thresholds are too strict for 7B. Stage 2 fake-int8 perturbation found one very sensitive norm output path (`layers.4.post_attention_layernorm`, +0.1809 loss delta), a sensitive early MLP down projection (`layers.3.mlp.down_proj`, +0.0265), moderate sensitivity for `layers.24.mlp.down_proj` (+0.0081), and mostly low deltas for late gate/up and attention controls. Projection-only outlier score correlated with absolute perturbation delta at about 0.78. Conclusion: the sensitivity-ranking idea partially transfers, but needs seed 43/44 replication and threshold recalibration before freezing a 7B selective policy. |
| 27 | 2026-05-19 | results | Completed H6.4 Qwen2.5-7B calibration-transfer replication on seeds 43 and 44. Stage 1 assignments were unchanged across seeds: all projections in the targeted panel remained bf16 and all norm/logit paths remained fp32. Stage 2 rankings were qualitatively stable: `layers.4.post_attention_layernorm` was consistently the most sensitive path (mean abs delta 0.2416), followed by `layers.3.mlp.down_proj` (0.0464). Four modules were consistently low-delta with max abs delta below 0.005: `layers.26.mlp.gate_proj`, `layers.26.mlp.up_proj`, `layers.27.mlp.gate_proj`, and `layers.26.self_attn.o_proj`. Projection-only outlier correlation remained about 0.78, while int8 relative MSE was non-predictive. Conclusion: different seeds do not alter the qualitative ranking; next freeze a conservative 7B rank/perturbation-selected fake-int8 policy and test it during LoRA updates. |
| 28 | 2026-05-19 | results | Completed H6.4 7B rank-selected fake-int8 training across seeds 42, 43, and 44. The policy fake-int8 quantized `layers.26.mlp.gate_proj`, `layers.26.mlp.up_proj`, `layers.27.mlp.gate_proj`, and `layers.26.self_attn.o_proj`. Against matched bf16 controls, eval-loss degradations were +0.161%, +0.223%, and +0.102%, with mean +0.162%, all far inside the 1% quality gate. All runs had zero loss spikes and zero NaN/Inf events. Memory was unchanged, as expected for fake-int8 output hooks, and the small apparent throughput gain should not be treated as a hardware claim. Conclusion: H6.4 supports calibration-to-training transfer at 7B for a conservative selected module set. |
| 29 | 2026-05-19 | synthesis | Wrote the final synthesis `to_human/final-synthesis-2026-05-19.md`. The synthesis concludes that H6 is supported as a calibration-guided sensitivity-ranking method, H6.3 is supported as a 7B QLoRA memory-capacity result, and H6.4 is supported as calibration-to-training transfer at 7B for a conservative selected fake-int8 module set. The project is ready for paper-style write-up. |
| 30 | 2026-05-20 | writeup | Began the paper-style write-up in `paper/calibration_guided_precision_assignment_draft.md`. The draft frames the completed evidence as calibration-guided sensitivity ranking and quality-preserving policy selection, while keeping hardware resource savings limited to the separate 7B QLoRA result. Added `paper/future_predictor_implementation_plan.md` to preserve the next implementation direction: a precision assignment predictor and budgeted optimizer. |
| 31 | 2026-05-20 | implementation | Implemented the first H7 precision predictor artifact. Added `experiments/h7-precision-predictor/` with a protocol, dataset builder, scikit-learn predictor trainer, and policy selector. Built `precision_dataset.csv` from H6 artifacts with 696 rows and 72 perturbation-labeled rows. The 0.5B-to-7B transfer check gives Ridge Spearman 0.536 and AUROC 0.776, modestly above outlier-only ranking at Spearman 0.484 and AUROC 0.750, with top-4 safe precision 1.0 versus 0.75. Leave-one-seed-out is mixed, so H7 is an early predictor extension rather than a confirmed replacement for outlier ranking. |
| 32 | 2026-05-20 | implementation | Added a staged Llama-3.1-8B rich rerun script at `experiments/h7-precision-predictor/code/run_llama31_8b_rich_h6.sh`. The script runs targeted three-seed H6-style calibration/perturbation probes, rebuilds the H7 predictor dataset across Qwen and Llama result roots, selects a Llama predictor policy, and optionally runs 500-step selected-policy, bf16, and QLoRA baselines. The protocol now records this as the cross-family H7 extension path. |
| 33 | 2026-05-21 | results | Completed the first matched 500-step Llama-3.1-8B training validation on A100 Colab for the conservative two-module fake-int8 policy (`layers.30.mlp.gate_proj`, `layers.30.mlp.up_proj`) against bf16 seed 42. BF16 reached eval loss 1.389425; the conservative policy reached 1.390033, a +0.000608 absolute or +0.044% relative degradation, far inside the 1% gate. Both runs had zero loss spikes and zero NaN/Inf events, peak memory was unchanged at 20.1955 GiB, and throughput changed from 278.56 to 277.00 train tokens/sec excluding first step. Conclusion: first cross-family Llama training validation is positive; replicate seeds 43 and 44 before testing wider policies. |
| 34 | 2026-05-22 | results | Completed Llama-3.1-8B conservative-policy replication on seeds 43 and 44. Across seeds 42, 43, and 44, bf16 eval losses were 1.389425, 1.364863, and 1.361036; the conservative two-module fake-int8 policy reached 1.390033, 1.366315, and 1.361887. Relative degradations were +0.044%, +0.106%, and +0.063%, with mean +0.071% and worst seed +0.106%, all far inside the 1% gate. All six runs had zero loss spikes and zero NaN/Inf events. Peak memory was unchanged and throughput deltas were small/noisy. Conclusion: H7 now supports replicated cross-family calibration-to-training transfer for a conservative Llama selected module set. |
| 35 | 2026-05-22 | results | Completed the Llama-3.1-8B cautious attention+MLP four-module policy on seed 42. The policy fake-int8 quantized `layers.30.self_attn.q_proj`, `layers.30.self_attn.o_proj`, `layers.30.mlp.gate_proj`, and `layers.30.mlp.up_proj`. Against the matched bf16 seed-42 eval loss 1.389425, the cautious policy reached 1.390232, a +0.000807 absolute or +0.058% relative degradation. It had zero loss spikes and zero NaN/Inf events, unchanged peak memory, and about 2.14% lower train throughput under fake-int8 hooks. Conclusion: the wider Llama policy is safe on seed 42; replicate seeds 43 and 44 before treating it as supported. |
| 36 | 2026-05-22 | audit | Checked the first seed 43/44 artifacts for the Llama cautious attention+MLP policy and found they are not valid replications. Both runs used only 100 steps instead of 500 and omitted `layers.30.self_attn.q_proj`, quantizing only three modules. These artifacts should not be compared against the 500-step bf16 baselines or used as evidence for the intended four-module policy. Next action is to rerun seeds 43 and 44 with all four modules and `--max-steps 500`. |
| 37 | 2026-05-23 | audit | Audited the lab 3090 cautious-policy rerun. The root-level `experiments/h7-precision-predictor/results/summary.json` is a valid seed-44, 500-step, four-module cautious-policy run on `rtx3090-lab`, with eval loss 1.362870, zero spikes, and zero NaN/Inf events. Compared to the existing A100 bf16 seed-44 baseline eval loss 1.361036, the quality delta is +0.135%, inside the 1% gate, but resource metrics are not comparable across hardware. No valid seed-43 RTX 3090 four-module artifact was found; the named seed-43 cautious directory is an A100 three-module run and remains misconfigured/exploratory. |
| 38 | 2026-05-23 | results | Found valid lab RTX 3090 cautious attention+MLP four-module artifacts for Llama-3.1-8B seeds 43 and 44 under `_rtx3090` output directories. Both use 500 steps, all four intended modules, and have zero loss spikes and zero NaN/Inf events. Combined with the earlier seed-42 A100 cautious run, eval degradations against available bf16 baselines are +0.058%, +0.044%, and +0.120%, with mean +0.074% and worst seed +0.120%. This supports the cautious four-module Llama policy on quality/stability across three seeds. Resource metrics are not comparable because seed 43/44 bf16 baselines are A100 while treatments are RTX 3090. |
| 39 | 2026-05-23 | literature | Refreshed literature and project notes for hardware-backed selective precision as a predictor-plus-optimizer problem. Added notes for HAQ, DNAS mixed precision, AWQ, OWQ, SpQR, SqueezeLLM, QServe, Ladder/BitBLAS, bitsandbytes, TorchAO, NVIDIA Transformer Engine, MS-AMP, LoftQ, QA-LoRA, PEQA, and AutoQRA. Updated the survey and findings to frame the best future direction as selective high-precision rescue from a hardware-backed low-bit baseline, using a `risk(module, format, backend)` predictor plus measured cost model rather than risk-only module ranking. |
| 40 | 2026-05-26 | results | Closed out H8 on Llama-3.1-8B with matched 500-step RTX 3090 comparisons across seeds 42-44. H8 selective bf16 projection rescue from QLoRA/NF4 improved final eval loss over blanket QLoRA on every seed by about 0.0016 absolute on average, while preserving a 25.28% peak-memory saving versus bf16. Blanket QLoRA saved 26.70% memory; H8 added 0.286 GiB peak memory for the rescued projections. Both H8 and QLoRA stayed inside the 1% quality gate with zero spikes and zero NaN/Inf events, but both remained about 19% slower than bf16. Conclusion: H8 is supported as a narrow memory-quality trade-off improvement, not as a throughput win. |
| 41 | 2026-05-26 | implementation | Bootstrapped H9 as a separate vLLM-first Transformer inference policy-search branch. Added protocol, README, analysis scaffold, policy-grid generator, backend inventory script, vLLM benchmark runner, prompt-logprob quality runner, and Pareto summarizer under `experiments/h9-transformer-inference-policy-search/`. Generated the initial 10-policy Llama-3.1-8B candidate grid and local backend inventory. The current shell has vLLM 0.11.2, bitsandbytes 0.48.1, flash-attn 2.8.3, and torchao 0.16.0 importable, but no visible CUDA device, so all policies are marked `not_tested` until run on the RTX 3090 host. |
| 42 | 2026-05-26 | results | Completed H9.1 vLLM inference policy search on Llama-3.1-8B using the lab RTX 3090. Seven runnable policies completed prompt-logprob quality scoring over 1202 tokens each. `fp16_default` is the clean supported candidate: quality changed by only +0.009% prompt NLL, memory was unchanged, prefill and mixed latency improved by about 0.5%, and decode latency was 0.27% slower. FP8 KV-cache policies passed quality but were dominated: they used about 3.6% more measured memory and were slightly slower. `fp16_bitsandbytes` improved decode-heavy throughput strongly but failed the 1% quality gate (+1.324% prompt NLL), was much slower on mixed workload, and had anomalous prefill generation. Conclusion: H9.1 is partially supported as a real vLLM policy-search workflow, but it does not yet find a strong memory-saving policy on RTX 3090. |
| 43 | 2026-05-26 | implementation | Started H9.2 long-context KV-cache stress. Added a `h9_2_long_context` profile to the H9 policy generator, producing a focused six-policy grid over bf16/fp16 defaults and FP8 KV-cache variants. Added long-context workloads `prefill_4k`, `decode_2k_context`, and `batch_mixed_long`, plus benchmark workload filtering and README commands. Generated `results/h9_2_long_context_policy_candidates.json` and dry-run benchmark artifacts under `results/h9_2_long_context_benchmarks/`. No H9.2 runtime benchmark has been run yet. |
| 44 | 2026-05-26 | results | Completed H9.2 long-context KV-cache stress on Llama-3.1-8B with the lab RTX 3090. All six focused policies completed all three workloads, giving 18 policy-workload rows and zero failures. FP8 KV cache still did not save memory: defaults used about 21.88 GiB, while FP8 KV policies used about 22.68 GiB, roughly +3.6%. `bf16_kv_fp8_e4m3` improved `prefill_4k` latency by 3.61% and `fp16_kv_fp8` improved `batch_mixed_long` by 2.06%, but both paid the memory cost. `bf16_default` remained best for `decode_2k_context`; `fp16_default` remained the cleanest low-risk policy with unchanged memory and small prefill/batch wins. The summary currently reuses H9.1 quality artifacts, so long-context-specific quality scoring remains before a final H9.2 quality claim. |
| 45 | 2026-05-26 | results | Completed H9.2 long-context prompt-logprob quality scoring after reducing quality scoring to one prompt per vLLM generate call and overriding quality-run GPU memory utilization to 0.78. All six H9.2 policies scored 7852 prompt tokens with zero missing logprobs. Prompt-NLL deltas versus `bf16_default` were tiny and inside the 1% quality gate: `fp16_default` -0.0148%, fp16 FP8 KV variants -0.0196%, and bf16 FP8 KV variants +0.0515%. This means H9.2's limiting factor is not quality; FP8 KV cache is mainly rejected because it increases measured memory by about 3.6% and does not help decode-context latency. |
| 46 | 2026-05-26 | synthesis | Added `paper/haq_principle_for_peft.md` to state the HAQ-to-PEFT adaptation explicitly. The note reframes HAQ's durable principle as hardware-aware precision assignment rather than RL over CNN bitwidths, defines PEFT rows as `(model, module, candidate_action, backend)` decisions, extends the risk target to `risk(module, format/action, backend)`, and frames the solver as a backend-feasible constrained assignment over real LoRA/QLoRA actions such as QLoRA/NF4, bf16 rescue, and adapter/backend precision states. Cross-linked the framing from the main paper draft discussion and related-work sections. |
| 47 | 2026-05-26 | implementation | Formalized the HAQ-to-PEFT adaptation as H10. Added `experiments/h10-haq-peft-assignment/protocol.md`, a grouped assignment solver at `experiments/h10-haq-peft-assignment/code/solve_precision_assignment.py`, and an example action table reconstructed from H8. Running the solver under a 1% quality-risk gate and required memory saving selects `qlora_nf4_bf16_projection_rescue` plus backend-default norm/logit handling, matching the intended H8-style selective-rescue policy. This is a method-assembly artifact rather than a new empirical training run. |
| 48 | 2026-05-26 | implementation | Replaced the H10 critical-path hand-written example with a generated action table. Added `experiments/h10-haq-peft-assignment/code/build_action_table.py`, which reads the H8 matched-comparison summary, filters to `rtx3090-lab` 500-step rows, aggregates QLoRA and selective-rescue quality/cost metrics across seeds 42-44, and writes `results/action_table.csv`. Solving that generated table again selects `qlora_nf4_bf16_projection_rescue` under the 1% quality-risk gate and required memory saving. |
| 49 | 2026-05-26 | implementation | Connected H10's generated backend-cost table to the precision-risk selector branch. Added `experiments/h10-haq-peft-assignment/code/build_selector_action_table.py`, which converts Llama selector candidates from `experiments/h10-peft-precision-risk/` into module-specific rescue actions and scales expected H8 quality recovery by each selector's unsafe recall. With the target perturbation upper-bound row included, the solver selects the perturbation-selected rescue set. Without that row, cheap selectors recover only one of three unsafe Llama projections at k=4 and tie; the first tied selection is activation-outlier rescue. This sharpens the conclusion that target perturbation checks, not pure cross-model prediction, are currently the defensible H10 selection method. |
| 50 | 2026-05-27 | report | Wrote the H10 progress memo `to_human/h10-haq-peft-progress-2026-05-27.md`. The memo frames the HAQ-to-PEFT adaptation as target perturbation-guided selective rescue from QLoRA/NF4, summarizes the H8 memory-quality trade-off, reports the generated H10 action table and selector-aware result, and states the claim boundary: supported assignment abstraction and target perturbation-guided rescue, but no pure learned predictor or throughput win yet. |
| 51 | 2026-05-27 | writeup | Updated `paper/calibration_guided_precision_assignment_draft.md` so H10 is part of the paper argument rather than only a future direction. Added a backend-aware assignment method stage, broadened the formulation to Qwen2.5 and Llama-3.1, added a backend-aware selective rescue experiment section with H8/H10 numbers, and clarified the discussion/limitations: target perturbation-guided rescue is the defensible HAQ-for-PEFT method, cross-model predictors remain extensions, and the current backend-real result is a memory-quality trade-off rather than a throughput win. |
| 52 | 2026-05-27 | literature | Checked the PyTorch TorchAO launch post against the active H10 inference-PTQ direction. It helps as candidate-space and ecosystem evidence: int4/int8 weight-only, dynamic int8, float8, KV-cache quantization, and `autoquant` all support H10's backend-aware action-table framing. It does not solve the local vLLM online TorchAO safetensors-loader failure, so the next empirical H10 path remains artifact-backed PTQ ingestion or a separately recorded TorchAO backend through Transformers/SGLang. |
| 53 | 2026-05-27 | results | Completed the active H10 inference-side PTQ table-and-solver result on matched Llama-3.1-8B-Instruct vLLM workloads. The final matched Marlin table selects `llama31_8b_instruct_gptq_marlin_artifact` under the strict 1% prompt-NLL gate. GPTQ-Marlin has a +0.773771% prompt-NLL delta versus matched bf16, while reducing latency by 62.72% on decode-heavy, 62.45% on mixed, and 60.49% on prefill-heavy workloads, with output throughput gains of 168.25%, 166.28%, and 153.14%. AWQ-Marlin is backend-feasible and fast but fails the strict gate at +2.851377% prompt-NLL, so it remains a relaxed-gate sensitivity result. Conclusion: H10 is supported in the active inference PTQ setting by a backend-real GPTQ-Marlin policy that passes the locked quality gate and improves the deployment frontier. |
| 54 | 2026-05-27 | writeup | Re-ran the CPU H10 matched Instruct Marlin table build and solver to verify reproducibility: the table rebuild produced 18 action rows with zero infeasible rows, the strict 1% solver selected 7 Pareto rows, and the relaxed 3% solver selected 8 Pareto rows. Updated `findings.md` and `paper/calibration_guided_precision_assignment_draft.md` so H10 is framed around inference-side PTQ, with the PEFT/QLoRA selector work archived as supporting evidence. The next research step is replication beyond one artifact/model family plus downstream task-quality checks beyond prompt NLL. |
| 55 | 2026-05-27 | implementation | Started the H10 task-quality follow-up. Added `experiments/h10-inference-ptq-assignment/code/run_h10_task_quality.py`, a small vLLM exact-match screen for arithmetic, numeric comparison, sentiment, sequence continuation, and yes/no prompts. Dry-ran it for matched `bf16_default`, `fp16_default`, and `llama31_8b_instruct_gptq_marlin_artifact`, producing task-quality JSON skeletons under `experiments/h10-inference-ptq-assignment/results/task_quality/`. Updated the H10 README and analysis with the CUDA command. This does not add new empirical quality evidence yet; it creates the next executable check for the strict GPTQ-Marlin claim. |
| 56 | 2026-05-28 | implementation | Prepared the H10 task-quality follow-up for an A100 fallback while the RTX 3090 host is occupied. Added `--run-label` to `run_h10_task_quality.py` so hardware-specific outputs can be isolated, and documented the A100 command in the H10 README and analysis. Boundary: A100 task-quality results are useful robustness evidence only when compared against matched A100 bf16/fp16 rows; A100 latency and memory must not be merged with the RTX 3090 deployment-frontier claim unless the full H9/H10 benchmark and prompt-NLL pipeline is rerun on A100. |
| 57 | 2026-05-28 | implementation | Added a Colab-safe Transformers fallback to `run_h10_task_quality.py` after A100 Colab vLLM failed with a tokenizer compatibility error and the GPTQ policy used a lab-local `/nfs` artifact path. The runner now supports `--runtime-backend transformers` for functional task-quality checks and `--model-override POLICY=MODEL` so `llama31_8b_instruct_gptq_marlin_artifact` can point to `shuyuej/Meta-Llama-3.1-8B-Instruct-GPTQ` on Colab. This fallback is only for task-quality robustness, not latency or memory claims. |
| 58 | 2026-05-28 | results | Completed the H10 task-quality sanity check on the lab RTX 3090 vLLM stack using GPU 7 and `run_label=rtx3090-lab`. `bf16_default`, `fp16_default`, and `llama31_8b_instruct_gptq_marlin_artifact` all completed with exact-match accuracy 0.6 and identical pass/fail patterns. All passed sentiment, sequence continuation, and yes/no prompts; all failed decimal comparison by answering `0.11` instead of `0.9`. Arithmetic was a scorer false negative because all policies produced a numerically correct `45` with extra formatting, so post-hoc numeric rescoring gives adjusted accuracy 0.8 for all three policies. Conclusion: the task screen shows no functional task-quality regression for GPTQ-Marlin relative to matched bf16/fp16 baselines. |
| 59 | 2026-05-30 | literature | Used the autoresearch literature workflow to collect papers for the soft-pruning-as-mixed-precision idea. Added a focused index on adaptive mixed-precision information allocation plus notes for ScaleBITS, SliM-LLM, Matryoshka Quantization, AdaQuantLM, QAQ, Prompt-Adaptive Quantization, CMPQ, FineQ, FGMP, and RAMP. The synthesis frames the idea as `precision = f(component sensitivity, prompt difficulty, hardware cost)`, with the main open systems problem being how to store heterogeneous bitwidths without losing savings to packing, metadata, alignment, and kernel limitations. |
| 60 | 2026-05-30 | literature | Added a review-quality filter for the soft-pruning/mixed-precision paper set. The core recommended set is SliM-LLM, Matryoshka Quantization, Bayesian Bits, HAQ, FracBits, and Instance-Aware Dynamic Quantization. QAQ, AdaQuantLM, and Prompt-Adaptive Quantization remain useful emerging OpenReview workshop references, but should not carry the main evidence claim. AutoMixQ and SAMPQ are excluded as main evidence because their OpenReview pages show withdrawn ICLR 2026 submissions. |
| 61 | 2026-05-30 | literature | Added an open-source-codebase filter for the soft-pruning/mixed-precision set. Recommended reproducible papers/projects are SliM-LLM, Bayesian Bits, HAQ, ChanMix, Instance-Aware Dynamic Quantization, DiffQ, and MatGPTQ. Marked FracBits, QAQ, AdaQuantLM, Prompt-Adaptive Quantization, ScaleBITS, FineQ, FGMP, RAMP, and CMPQ as relevant but without a public codebase found in this pass. |
| 62 | 2026-05-30 | literature | Checked MoBiQuant and added it as a highly related emerging paper. It directly matches token-adaptive mixed-bit LLM inference using many-in-one recursive residual quantization plus a token-aware router. It is especially relevant to the storage problem because higher precision is reconstructed from residual bit slices rather than separate bit-specific checkpoints. Caveat: current search found it as an arXiv 2026 preprint with no public codebase found. |
| 63 | 2026-05-30 | literature | Used MoBiQuant as the seed paper for another autoresearch literature pass. Added a MoBiQuant-like survey and notes for DP-LLM, Any-Precision LLM, QuEPT, QAQ KV Cache, Don't Waste Bits, ARKV, MatGPTQ, and NestedFP. The closest aligned cluster is runtime or elastic precision assignment: MoBiQuant routes tokens to residual bit slices, DP-LLM routes layers by input-conditioned error, ARKV/Don't Waste Bits/QAQ route KV-cache precision by token/cache importance, and Any-Precision LLM/MatGPTQ/NestedFP solve the single-checkpoint multi-precision storage problem. |
